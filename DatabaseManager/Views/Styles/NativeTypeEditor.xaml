<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:converters="clr-namespace:DatabaseManager.Views.Converters"
    xmlns:myControls="clr-namespace:DatabaseManager.Views.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ToggleSwitch.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters:ObjectToDoubleConverter x:Key="ObjectToDoubleConverter" />
    <converters:ObjectToCharConverter x:Key="ObjectToCharConverter" />

    <Style TargetType="{x:Type myControls:NativeTypeEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type myControls:NativeTypeEditor}">
                    <StackPanel x:Name="StackPanel">
                        <TextBlock
                            x:Name="NameTextBlock"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="Center"
                            Background="{DynamicResource ControlBackgroundBrush}"
                            FontFamily="{TemplateBinding FontFamily}"
                            FontSize="10"
                            FontStretch="{TemplateBinding FontStretch}"
                            FontStyle="{TemplateBinding FontStyle}"
                            FontWeight="{TemplateBinding FontWeight}"
                            Foreground="{DynamicResource TextBrush}"
                            Text="{TemplateBinding PropertyName}"
                            TextWrapping="Wrap" />

                        <Grid>
                            <TextBox
                                Name="TextBox"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                MinWidth="{TemplateBinding MinWidth}"
                                MaxWidth="{TemplateBinding MaxWidth}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                controls:TextBoxHelper.ClearTextButton="True"
                                Background="{DynamicResource ControlBackgroundBrush}"
                                CaretBrush="{DynamicResource AccentBaseColorBrush}"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding FontSize}"
                                FontStretch="{TemplateBinding FontStretch}"
                                FontStyle="{TemplateBinding FontStyle}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{TemplateBinding Foreground}"
                                MaxLength="{TemplateBinding MaxTextLength}"
                                Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Mode=TwoWay}"
                                TextWrapping="Wrap"
                                Visibility="Collapsed" />

                            <controls:NumericUpDown
                                Name="NumericUpDown"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                MinWidth="{TemplateBinding MinWidth}"
                                MaxWidth="{TemplateBinding MaxWidth}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Background="{DynamicResource ControlBackgroundBrush}"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding FontSize}"
                                FontStretch="{TemplateBinding FontStretch}"
                                FontStyle="{TemplateBinding FontStyle}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{TemplateBinding Foreground}"
                                Maximum="{TemplateBinding MaxValue}"
                                Minimum="{TemplateBinding MinValue}"
                                Visibility="Collapsed"
                                Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Mode=TwoWay, Converter={StaticResource ObjectToDoubleConverter}}" />

                            <controls:ToggleSwitch
                                Name="ToggleSwitch"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                MinWidth="{TemplateBinding MinWidth}"
                                MaxWidth="{TemplateBinding MaxWidth}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Mode=TwoWay}"
                                OffLabel=""
                                OnLabel=""
                                Style="{StaticResource ToggleSwitchStyle}"
                                Visibility="Collapsed" />
                        </Grid>
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Name" Value="">
                            <Setter TargetName="NameTextBlock" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="UseBigName" Value="True">
                            <Setter TargetName="StackPanel" Property="Orientation" Value="Horizontal" />
                            <Setter TargetName="NameTextBlock" Property="FontSize" Value="15" />
                            <Setter TargetName="NameTextBlock" Property="Margin" Value="0,0,5,0" />
                        </Trigger>

                        <Trigger Property="Type" Value="Bool">
                            <Setter TargetName="ToggleSwitch" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="Type" Value="String">
                            <Setter TargetName="TextBox" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Type" Value="Char">
                            <Setter TargetName="TextBox" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="Type" Value="Byte">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>
                        <Trigger Property="Type" Value="SByte">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>

                        <Trigger Property="Type" Value="Short">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>
                        <Trigger Property="Type" Value="UShort">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>

                        <Trigger Property="Type" Value="Int">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>
                        <Trigger Property="Type" Value="UInt">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>

                        <Trigger Property="Type" Value="Long">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>
                        <Trigger Property="Type" Value="ULong">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                            <Setter TargetName="NumericUpDown" Property="HasDecimals" Value="False" />
                        </Trigger>

                        <Trigger Property="Type" Value="Float">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="Type" Value="Decimal">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="Type" Value="Double">
                            <Setter TargetName="NumericUpDown" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>